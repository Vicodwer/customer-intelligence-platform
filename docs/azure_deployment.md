# Azure Deployment Guide

## Goal

Deploy the Customer Intelligence Platform Docker image to Azure using:

- Azure Container Registry
- Azure Container Apps

This document records the intended deployment path and commands.

---

## Prerequisites

- Azure subscription
- Azure CLI installed
- Docker installed
- Logged in with:

```powershell
az login